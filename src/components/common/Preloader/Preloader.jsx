import React from "react";
import preloader from '../../../assets/images/preloader.svg';

const Preloader = (props) => {
    return <img src={preloader} style={{ width: 100 }} alt="" />
}

export default Preloader;